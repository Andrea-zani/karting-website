//SCRIPT TO CHANGE TILTE INDEX NEWS AND NEWS IN THE MEANING TIME
$(document).ready(function() {
  //should be no longer than 47 characters included white spaces to fit in the news page div
  var title = "this is the new title";
  //var length = title.length;
  //alert(length);
  $(".script").text(title);
  
});
